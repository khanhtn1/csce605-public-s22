// test017.txt
// Copy propagation test
// Adapted from Muchnick, p.357
main
var a, b, c, d, e;
{
	let a := 1;
	let b := a;
	let c := 4 * b;
	if c > b then
		let d := b + 2
	fi;
	let e := a + b;
	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
	call OutputNum(d);
	call OutputNum(e);
}
.